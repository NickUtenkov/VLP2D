<UserControl x:Class="VLP2D.View.OutputView"
	xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
	xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
	xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
	xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
	xmlns:i="http://schemas.microsoft.com/xaml/behaviors"
	xmlns:Behaviors="clr-namespace:VLP2D.Common"
	xmlns:ViewModel="clr-namespace:VLP2D.ViewModel"
	xmlns:local="clr-namespace:VLP2D.View"
	xmlns:loc="clr-namespace:VLP2D.Properties"
	mc:Ignorable="d" 
	d:DesignHeight="300" d:DesignWidth="300">

	<UserControl.DataContext>
		<ViewModel:OutputVM/>
	</UserControl.DataContext>

	<Grid>
		<Grid.RowDefinitions>
			<RowDefinition Height="25"/>
			<RowDefinition Height="*"/>
			<RowDefinition Height="Auto"/>
		</Grid.RowDefinitions>
		<StackPanel Grid.Row="0" Orientation="Horizontal" HorizontalAlignment="Center">
			<TextBlock Text="{x:Static loc:Resources.strResults}" FontSize="18" IsHitTestVisible="false" HorizontalAlignment="Center"/>
			<Button Height="23" Width="65" Margin="3,1,0,0" Content="{Binding strButton2D3D}"
				HorizontalAlignment="Center" VerticalAlignment="Top"
				Command="{Binding switchMode2D3DCommand}"
				>
			</Button>
		</StackPanel>
		<Border Grid.Row="1" Background="{x:Null}" BorderThickness="1" BorderBrush="#AAAAAA" Margin="1,0,1,0">
			<Grid Height="Auto" Width="Auto" Background="Transparent">
				<i:Interaction.Behaviors>
					<Behaviors:ReadOnlyPropertyToModelBindingBehavior ModelProperty="{Binding gridHeight}" ReadOnlyDependencyProperty="{Binding ActualHeight, RelativeSource={RelativeSource Mode=FindAncestor, AncestorType=Grid}}" />
					<Behaviors:ReadOnlyPropertyToModelBindingBehavior ModelProperty="{Binding gridWidth}" ReadOnlyDependencyProperty="{Binding ActualWidth, RelativeSource={RelativeSource Mode=FindAncestor, AncestorType=Grid}}" />
				</i:Interaction.Behaviors>
				<i:Interaction.Triggers>
					<i:EventTrigger EventName="MouseLeftButtonDown">
						<i:InvokeCommandAction Command="{Binding mouseLeftButtonDown}" PassEventArgsToCommand="True"/>
					</i:EventTrigger>
					<i:EventTrigger EventName="MouseMove">
						<i:InvokeCommandAction Command="{Binding mouseMove}" PassEventArgsToCommand="True"/>
					</i:EventTrigger>
					<i:EventTrigger EventName="MouseLeftButtonUp">
						<i:InvokeCommandAction Command="{Binding mouseLeftButtonUp}" PassEventArgsToCommand="True"/>
					</i:EventTrigger>
				</i:Interaction.Triggers>
				<Grid Visibility="{Binding Path=bShow3D, Converter={StaticResource BoolToNoVisibilityConverterKey}}">
					<Grid.ColumnDefinitions>
						<ColumnDefinition Width="Auto"/>
						<ColumnDefinition Width="Auto"/>
					</Grid.ColumnDefinitions>
					<Image Grid.Column="0" Source="{Binding heatMap}"/>
					<Image Grid.Column="1" Source="{Binding heatMapDiff}" Margin="5,0,0,0">
						<Image.LayoutTransform>
							<ScaleTransform ScaleX="1" ScaleY="{Binding scaleYDif}"/>
						</Image.LayoutTransform>
					</Image>
				</Grid>
				<Viewport3D Visibility="{Binding Path=bShow3D, Converter={StaticResource BoolToVisibilityConverterKey}}">
					<Viewport3D.Camera>
						<OrthographicCamera x:Name="camera" Width="2"
						FarPlaneDistance="10"
						NearPlaneDistance="1" 
						LookDirection="0,0,-1"
						UpDirection="0,1,0"
						Position="0,0,2" />
					</Viewport3D.Camera>
					<ModelVisual3D x:Name="Light1">
						<ModelVisual3D.Content>
							<DirectionalLight Color="White" Direction="1, 1, -1" />
						</ModelVisual3D.Content>
					</ModelVisual3D>
					<ModelVisual3D x:Name="Light2">
						<ModelVisual3D.Content>
							<DirectionalLight Color="White" Direction="-1, 1, -1" />
						</ModelVisual3D.Content>
					</ModelVisual3D>
					<ModelVisual3D x:Name="Light3">
						<ModelVisual3D.Content>
							<DirectionalLight Color="White" Direction="0,-1,-0.5" />
						</ModelVisual3D.Content>
					</ModelVisual3D>
					<ModelVisual3D Content="{Binding modelSurface}"/>
				</Viewport3D>
				<StackPanel Orientation="Vertical" Visibility="{Binding Path=bShow3D, Converter={StaticResource BoolToVisibilityConverterKey}}"
					HorizontalAlignment="Right" VerticalAlignment="Center">
					<Button Height="23" Width="23" Content="+" Command="{Binding zoomInCommand}"/>
					<Button Height="23" Width="23" Content="-" Command="{Binding zoomOutCommand}"/>
					<Button Height="23" Width="23" Content="H" Command="{Binding homeCommand}"/>
				</StackPanel>
			</Grid>
		</Border>
		<StackPanel Grid.Row="2">
			<StackPanel Visibility="{Binding Path=bShowSlider, Converter={StaticResource BoolToVisibilityConverterKey}}">
				<Slider Maximum="{Binding sliderTicks, Mode=TwoWay}" Minimum="0" TickFrequency="1" TickPlacement="None"
					Value="{Binding idxHeatMap, Mode=TwoWay}" x:Name="mySlider" LargeChange="{Binding sliderLargeChange, Mode=TwoWay}"
					 IsSnapToTickEnabled="True"
					>
				</Slider>
				<StackPanel Orientation="Horizontal">
					<Label Content="{x:Static loc:Resources.strIndex}" />
					<TextBlock Text="{Binding Value, ElementName=mySlider, StringFormat={}{0:0}}" VerticalAlignment="Center"/>
					<Label Content="{x:Static loc:Resources.strIterations}" Margin="5,0,0,0"
						Visibility="{Binding Path=showIterations, Converter={StaticResource BoolToVisibilityConverterKey}}"
					/>
					<TextBlock Text="{Binding allIters}" VerticalAlignment="Center"/>
					<Label Content="{x:Static loc:Resources.strCalcTime}" Margin="5,0,0,0"/>
					<TextBlock Text="{Binding allTime}" VerticalAlignment="Center"/>
					<Label Content="{x:Static loc:Resources.strDeviation}" Margin="5,0,0,0" Visibility="{Binding Path=showDeviation, Converter={StaticResource BoolToVisibilityConverterKey}}"/>
					<TextBlock Text="{Binding deviation}" VerticalAlignment="Center" Visibility="{Binding Path=showDeviation, Converter={StaticResource BoolToVisibilityConverterKey}}"/>
				</StackPanel>
			</StackPanel>
			<StackPanel Visibility="{Binding Path=bShowProgress, Converter={StaticResource BoolToVisibilityConverterKey}}">
				<Grid>
					<ProgressBar Name="progress" Minimum="0" Maximum="100" Value="{Binding progressValue}" Height="20"/>
					<TextBlock HorizontalAlignment="Center" VerticalAlignment="Center" Foreground="#FFFF0000">
						<Run Text="{Binding progressHeader}"></Run>
						<Run Text="{Binding ElementName=progress, Path=Value, StringFormat={}{0:#,#.00}%}"></Run>
					</TextBlock>
				</Grid>
				<Button Height="25" Width="130" Margin="0,5,0,5"
					Command="{Binding allIterationsCancelCommand}" Style="{StaticResource btnRedStyle}"
					HorizontalAlignment="Center" VerticalAlignment="Center"
					>
					<TextBlock Text="{x:Static loc:Resources.strCancel}"/>
				</Button>
			</StackPanel>
		</StackPanel>
		<TextBlock Grid.Row="2" Text="{Binding elapsedInfo}" FontSize="15" Margin="-255,-240,0,0" IsHitTestVisible="False"/>
	</Grid>

</UserControl>
